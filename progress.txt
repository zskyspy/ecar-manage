GarageFlow — Build Progress Log
================================
Started: 2026-09-03

[2026-09-03] Session start
- Read PLAN.txt and dev TOOLS.txt to understand project scope and stack
- Confirmed workspace contains planning docs only (no Django project yet)
- Created this progress.txt file to track all work done
- Ran environment check on user's machine:
  * Python 3.14.4 — installed (Django 6.1 also already installed via pip)
  * pip 26.1.2 — installed
  * Git — NOT installed
  * PostgreSQL (psql) — NOT installed
  * Docker — NOT installed
- Advised user on required manual installs before Step 1 can begin

[2026-09-03] Rechecked tools after user install
- Git 2.55.0.windows.5 — installed and working
- PostgreSQL 18.6 — installed (not 16 as originally planned; 18 is fine)
- Windows service postgresql-x64-18 — Running
- psql.exe lives at C:\Program Files\PostgreSQL\18\bin\psql.exe
- psql is NOT on PATH in PowerShell, so commands must use the full path (or add that bin folder to PATH)
- Postgres login as user postgres succeeded
- Created database: garageflow

[2026-09-03] Steps 1–3: environment, DB, Django skeleton
- Created Python venv at ./venv
- Installed in venv: Django 6.1.1, django-environ 0.14.0, psycopg2-binary 2.9.12
- Scaffolded Django project garageapp (manage.py at repo root)
- Created Django app: jobs
- Added .gitignore (venv, .env, pycache, etc.)
- Added .env with local DB credentials (gitignored) and .env.example without secrets
- Added requirements.txt
- Pointed settings.py at PostgreSQL via django-environ (no hardcoded DB password in settings)
- Registered jobs in INSTALLED_APPS
- Added home (/) and health (/health/) endpoints; health hits SELECT 1 on Postgres
- python manage.py check — no issues
- python manage.py migrate — Django default tables created in garageflow DB
- git init — empty repo created (no commit yet)
- Confirmed locally: GET /health/ returns {"status":"ok","app":"GarageFlow","database":"connected"}
- Confirmed locally: GET / returns "GarageFlow is running."
- Dev server left running at http://127.0.0.1:8000/

[2026-09-03] Baseline Git Commit (Steps 1-3)
- Configured local git user
- Committed baseline project files (commit d56b701)

[2026-09-03] Step 4: Auth Layer
- Added djangorestframework and djangorestframework-simplejwt to requirements.txt and installed in venv
- Configured rest_framework and SIMPLE_JWT in garageapp/settings.py (JWTAuthentication + SessionAuthentication)
- Created UserProfile model in jobs/models.py with Role choices ('owner', 'technician') and helper properties
- Connected post_save signal on User model to guarantee auto-provisioning of UserProfile
- Registered UserProfile in jobs/admin.py as inline with UserAdmin
- Created custom DRF permissions in jobs/permissions.py: IsOwner and IsTechnician
- Created serializers in jobs/serializers.py: UserProfileSerializer, UserSerializer, CustomTokenObtainPairSerializer (embeds role and username into JWT claims and login response)
- Added views and routes:
  * POST /api/token/ — issues JWT access & refresh tokens with custom claims
  * POST /api/token/refresh/ — refreshes access token
  * GET /api/auth/me/ — retrieves current user profile and role (authenticated)
  * GET /api/auth/test-owner/ — owner-only protected route
  * GET /api/auth/test-technician/ — technician-only protected route
- Generated and applied migration jobs/migrations/0001_initial.py
- Automated tests: created 5 unit tests in jobs/tests.py covering profile auto-creation, token issuance with claims, profile retrieval, and role enforcement (200, 401, 403 status codes)
- Ran manage.py test jobs — 5/5 tests passed on PostgreSQL
- Seeded dev users: owner (role: owner) and tech (role: technician)
- Live manual verification via REST requests: confirmed token generation, payload decoding, 200 on matching role, and 403 on mismatched role

[2026-09-03] Step 5: Core Job CRUD
- Added django-filter to requirements.txt and installed in venv (v26.1)
- Added django_filters to INSTALLED_APPS and configured DEFAULT_FILTER_BACKENDS (DjangoFilterBackend, SearchFilter, OrderingFilter) in settings.py
- Created Job model in jobs/models.py with fields: customer_name, customer_phone, vehicle_make, vehicle_model, vehicle_year, license_plate, vin, description, status (Status choices: pending, in_progress, waiting_parts, completed, cancelled), assigned_technician, created_by, timestamps
- Registered Job and JobAdmin in jobs/admin.py with list_display, list_filter, search_fields, and ordering
- Created JobFilter in jobs/filters.py with exact and case-insensitive filter lookups
- Created JobSerializer in jobs/serializers.py with read-only audit fields and assigned technician / creator usernames
- Implemented JobViewSet in jobs/views.py with search, ordering, filter backends, and automatic created_by binding on create
- Registered JobViewSet with DRF DefaultRouter under /api/jobs/
- Generated and applied migration jobs/migrations/0002_job.py
- Added 7 automated tests in jobs/tests.py (12 total in suite):
  * test_create_job (POST /api/jobs/)
  * test_list_jobs (GET /api/jobs/)
  * test_retrieve_job (GET /api/jobs/{id}/)
  * test_partial_update_job (PATCH /api/jobs/{id}/)
  * test_delete_job (DELETE /api/jobs/{id}/)
  * test_filter_jobs_by_status (?status=in_progress)
  * test_search_jobs (?search=...)
- Ran manage.py test jobs — all 12/12 tests passed on PostgreSQL
- Live manual verification via REST requests: confirmed POST /api/jobs/, GET /api/jobs/{id}/, PATCH /api/jobs/{id}/, and query filtering with JWT auth

Notes / decisions
- Python 3.14 + Django 6.1 used instead of Python 3.12 + Django 5.x from the original tools list (3.12 is not installed; 6.1 is the current compatible line)
- PostgreSQL 18 used instead of 16 (what the installer provided)
- DB password is only in .env, never in progress.txt or settings.py
- Role access control enforced at the permission/backend layer via IsOwner / IsTechnician
- Job CRUD routes live under /api/jobs/ with DRF DefaultRouter


